const minimumFunction = (a, b) => (a < b ? a : b);
const maximumFunction = (a, b) => (a > b ? a : b);
const sumFunction = (a, b) => a + b;

/**
 * Redukciós függvények gyűjteménye.
 */
export const ReducerFunctions = {
    minimumFunction,
    maximumFunction,
    sumFunction
};